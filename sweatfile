[direnv]
# TODO(amarbel-llc/spinclass#19): switch to dotenv when supported
# dotenv = { MOXY_BUILTIN_DIR = "$WORKTREE/build/builtin-servers" }
envrc = ["source_up", "use flake", "export MOXY_BUILTIN_DIR=$PWD/build/builtin-servers"]

[hooks]
pre-merge = "just"
