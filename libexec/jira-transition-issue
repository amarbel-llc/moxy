#!/usr/bin/env bash
set -euo pipefail
issue_key="$1"
status="$2"
output_format="${3:-}"
args=(jira workitem transition "$issue_key" --status "$status" --yes)
[ "$output_format" = "json" ] && args+=(--json)
acli "${args[@]}"
