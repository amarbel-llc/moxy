#!/usr/bin/env bash
set -euo pipefail
project="${1:-}"
name="${2:-}"
max_results="${3:-50}"
args=(jira board list --limit "$max_results" --json)
[ -n "$project" ] && args+=(--project "$project")
[ -n "$name" ] && args+=(--name "$name")
acli "${args[@]}"
