schema = 3
result-type = "text"
content-type = "text/plain"
cache-results = "always"
perms-request = "always-allow"
permit-async = false
description = "List all available man pages from MANPATH. Returns page names with short descriptions, sorted alphabetically."
command = "@BIN@/list"

[input]
type = "object"

[annotations]
read-only-hint = true
idempotent-hint = true
