#!/usr/bin/env bash
set -euo pipefail
message="$1"
amend="${2:-}"
repo="${3:-.}"
cd "$repo"
git_args=(commit)

[ "$amend" = "true" ] && git_args+=(--amend)
git_args+=(-m "$message")
git "${git_args[@]}"
