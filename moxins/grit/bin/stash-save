#!/usr/bin/env bash
set -euo pipefail
message="${1:-}"
include_untracked="${2:-}"
repo="${3:-.}"
cd "$repo"
git_args=(stash push)

[ "$include_untracked" = "true" ] && git_args+=(-u)

[ -n "$message" ] && git_args+=(-m "$message")
git "${git_args[@]}"
