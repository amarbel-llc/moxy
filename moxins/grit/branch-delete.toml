schema = 3
description = "Delete a local branch"
command = "@BIN@/branch-delete"
arg-order = ["name", "force", "repo_path"]

[input]
type = "object"
required = ["name"]

[input.properties.name]
type = "string"
description = "Name of the branch to delete"

[input.properties.force]
type = "boolean"
description = "Force delete even if not fully merged (-D instead of -d)"

[input.properties.repo_path]
type = "string"
description = "Path to the git repository (defaults to current working directory — almost never needed)"

[annotations]
destructive-hint = true
