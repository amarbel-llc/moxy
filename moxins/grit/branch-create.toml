schema = 3
perms-request = "always-allow"
description = "Create a new branch"
command = "@BIN@/branch-create"
arg-order = ["name", "start_point", "repo_path"]

[input]
type = "object"
required = ["name"]

[input.properties.name]
type = "string"
description = "Name for the new branch"

[input.properties.start_point]
type = "string"
description = "Starting point for the new branch (commit, branch, tag)"

[input.properties.repo_path]
type = "string"
description = "Path to the git repository (defaults to current working directory — almost never needed)"
