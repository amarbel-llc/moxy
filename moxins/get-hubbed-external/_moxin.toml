schema = 1
name = "get-hubbed-external"
description = "GitHub tools for repositories OTHER than the current one. Every tool requires an explicit repo parameter in OWNER/REPO format. For current-repo operations, use get-hubbed tools instead."
