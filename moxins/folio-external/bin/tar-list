#!/usr/bin/env bash
set -euo pipefail
path="$1"
pattern="${2:-}"

if [ ! -f "$path" ]; then
  echo "Error: not a file: $path" >&2
  exit 1
fi

if [ -n "$pattern" ]; then
  tar -tf "$path" | grep -E "$pattern"
else
  tar -tf "$path"
fi
