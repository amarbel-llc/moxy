schema = 3
description = """Extract and print a single file from a tarball to stdout. \
Use tar-list first to discover available members."""
command = "@BIN@/tar-cat"
arg-order = ["path", "member"]

[input]
required = ["path", "member"]

[input.properties.path]
type = "string"
description = "Absolute path to the tarball."

[input.properties.member]
type = "string"
description = "Path of the member to extract (e.g. 'moxy/bin/moxy')."

[annotations]
read-only-hint = true
idempotent-hint = true
