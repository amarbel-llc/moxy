#!/usr/bin/env bash
set -euo pipefail
number="$1"
body="$2"
repo="${3:-}"
host="${4:-}"

# shellcheck source=bin/.smith-common
. "$(dirname "$0")/.smith-common"

ref=$(qualify_ref "$number" "$repo")
smith_base "$host"
smith_run "$repo" "$host" issue comment "$ref" "$body"
