#!/usr/bin/env bash
set -euo pipefail
number="$1"
repo="${2:-}"
host="${3:-}"

# shellcheck source=bin/.smith-common
. "$(dirname "$0")/.smith-common"

ref=$(qualify_ref "$number" "$repo")
smith_base "$host"
smith_run "$repo" "$host" pr status "$ref"
