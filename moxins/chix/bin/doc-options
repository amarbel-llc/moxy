#!/usr/bin/env bash
set -euo pipefail

query="${1:?query required}"
strict="${2:-}"

args=("$query" --source nixos-options --source hm-options --source nd-options)
if [ "$strict" = "true" ]; then
  args+=(--strict)
fi

manix "${args[@]}"
