schema = 3
result-type = "text"
perms-request = "dynamic"
description = """Extract and print a single file from a tarball to stdout. \
Use tar-list first to discover available members."""
command = "@BIN@/tar-cat"
arg-order = ["path", "member"]

[input]
required = ["path", "member"]

[input.properties.path]
type = "string"
description = "Path to the tarball. Auto-allowed inside CWD, /nix/store, and Claude-native dirs; prompts elsewhere."

[input.properties.member]
type = "string"
description = "Path of the member to extract (e.g. 'moxy/bin/moxy', 'moxy/share/moxy/moxins/folio/read.toml')."

[annotations]
read-only-hint = true
idempotent-hint = true

[dynamic-perms]
command = "@BIN@/folio-perms"
args = ["read"]
arg-order = ["path"]
timeout-ms = 1000
