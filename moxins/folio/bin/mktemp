#!/usr/bin/env bash
set -euo pipefail
directory="${1:-false}"
suffix="${2:-}"

tmpdir="$PWD/.tmp"
mkdir -p "$tmpdir"

flags=(-p "$tmpdir")
[[ "$directory" == "true" ]] && flags+=(-d)
[[ -n "$suffix" ]] && flags+=(--suffix="$suffix")

mktemp "${flags[@]}"
