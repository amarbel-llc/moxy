#!/usr/bin/env bash
set -euo pipefail
path="$1"
mode="$2"

if [ ! -e "$path" ] && [ ! -L "$path" ]; then
  echo "no such file or directory: $path" >&2
  exit 1
fi

chmod "$mode" -- "$path"
echo "chmod $mode $path"
