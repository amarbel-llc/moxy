schema = 3
result-type = "text"
content-type = "text/plain"
perms-request = "always-allow"
description = "List all targets currently registered with gh-watch, probing each for live state via the GitHub API. Returns a table with handle, kind, current status, conclusion, and age. Read-only."
command = "@BIN@/watch-list"
arg-order = []

[input]
type = "object"

[annotations]
read-only-hint = true
idempotent-hint = true
open-world-hint = true
