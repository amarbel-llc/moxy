schema = 1
perms-request = "always-allow"
description = "List available recipes with their descriptions, grouped by category. Shows recipe names, parameters, and doc comments. Includes recipes from child justfiles in subdirectories, shown with dir/recipe prefix."
command = "@LIBEXEC@/just-list-recipes"

[input]
type = "object"

[annotations]
read-only-hint = true
idempotent-hint = true
